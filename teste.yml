---
- name: Coletar informações dos PCs
  hosts: all
  gather_facts: yes

  tasks:

    - name: Mostrar nome do computador
      debug:
        msg:
          - "Hostname: {{ ansible_hostname }}"
          - "Sistema: {{ ansible_distribution }}"
          - "IP: {{ ansible_default_ipv4.address }}"
