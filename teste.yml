---
- name: Instalar impressora Central Piso 2
  hosts: all
  gather_facts: no

  tasks:

    - name: Adicionar impressora
      ansible.windows.win_shell: |
        rundll32 printui.dll,PrintUIEntry /in /n "\\192.168.15.131\Central Piso 2" 
      async: 30
      poll: 0
