- name: Instalar Tasy
  hosts: all
  gather_facts: no

  tasks:

    - name: Instalar Tasy direto
      ansible.windows.win_package:
        path: C:\TEMP\TasyNative-5.14.7-win-x64.msi
        state: present
        arguments: "/passive /norestart"
