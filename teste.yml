- hosts: all
  gather_facts: no

  tasks:

    - name: Instalar Tasy (via msiexec)
      win_shell: |
        msiexec /i C:\TEMP\TasyNative-5.14.7-win-x64.msi /quiet /norestart /l*v C:\TEMP\install.log
