- name: Coletar informações do Windows
  hosts: meu_pc
  tasks:
    - name: Rodar systeminfo
      win_shell: systeminfo
      register: resultado

    - name: Mostrar resultado
      debug:
        var: resultado.stdout
