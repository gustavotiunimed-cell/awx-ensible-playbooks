---
- name: Testar servidor de impressão
  hosts: all
  gather_facts: no

  tasks:

    - name: Listar compartilhamento de impressoras
      ansible.windows.win_shell: |
        Get-ChildItem "\\192.168.15.131"
      register: resultado

    - name: Mostrar resultado
      debug:
        var: resultado.stdout_lines
